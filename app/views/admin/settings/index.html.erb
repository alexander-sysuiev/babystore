<h1>Settings List</h1>
<ol>
  <% SettingName.all.each do |setting| %>
    <li><%= link_to setting.name, edit_admin_setting_path(setting) %></li>
  <% end %>
</ol>

<%= link_to 'Add setting', new_admin_setting_path %>
