<% if @items.any? %>
  <div class="goods-preview_list">
    <div class="goods-preview">
	    <% @items.each do |product| %>
	      <div class="item-preview">
	      	<%= link_t(image_tag(product.image.photo.url(:thumb), :class => "item-image"), product_path(product)) if product.image.present? %>
	      	<div><%= format_price product.price %></div>
	      	<%= product_link(product) %>
	      </div>
	    <% end %>
	</div>
  </div>
<% else %>
  <h1>No items in database</h1>
<% end %>
